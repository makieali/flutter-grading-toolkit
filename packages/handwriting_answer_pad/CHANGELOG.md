## 0.1.0

- Initial release.
- `HandwritingAnswerPad` widget: scribble canvas with undo/redo/clear and a
  recognize action driven by a pluggable `HandwritingRecognizer`.
- `HandwritingPadController` for programmatic control and render-to-PNG.
